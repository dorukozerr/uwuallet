import { Puff } from "react-loading-icons";

const Loading = () => (
  <div className="flex h-full w-full items-center justify-center">
    <Puff className="h-[1.2rem] w-[1.2rem]" />
  </div>
);

export default Loading;
